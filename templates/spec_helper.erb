require 'chefspec'
require_relative '../spec_helper'

describe '<%= @cookbook %>::<%= @recipe %>' do
  include_context('chef recipe run', '<%= @cookbook %>::<%= @recipe %>')

  it 'runs successfully' do
    expect{chef_run}.not_to raise_error
  end
end
